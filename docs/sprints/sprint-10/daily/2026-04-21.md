# Daily — Sprint 10 — 2026-04-21

## Completado hoy

### Bitácora unificada (EXP-BIT-001)
- Endpoint `GET /expedientes/{id}/actividad` — agrega todo el historial retrocompatible
- Fix raíz: `ModuleNotFoundError` por `app.models.vencimiento` (vive en `app.models.expediente`)
- Fix serialización: enums con `.value`, Decimal con `float()`
- Frontend: feed con iconos/colores por tipo de evento, botón ↻, Enter para registrar movimiento
- Callbacks `onCreated` en `HonorariosTab`, `TareasSection`, `DocumentosTab`

### Documentos avanzados (DOC-002 + DOC-003)
- `label` editable inline, `orden` con botones ↑↓
- Backend proxy `GET /documentos/{id}/content` (httpx + StreamingResponse) — fix iframe y descarga
- `GET /documentos/merged-pdf` con pypdf — botón "Descargar todo"
- Migración `f5e21ed7929a` con `server_default='0'` para filas existentes

### Trial + APScheduler (TRIAL-001 + NOTIF-001)
- `trial_ends_at` en Studio, migración `2ceee8661236`, banner en layout
- BackgroundScheduler CronTrigger 9am para emails urgentes diarios

### UX detalle expediente
- Badges de resumen en colapsables (honorarios, vencimientos, tareas, docs)
- "Resumen IA · Beta" al tope de columna derecha, grisado
- Fix "0 días": comparar fechas calendario ART, no milisegundos exactos

## Bugs producción resueltos
- PDF preview blank + descarga UUID → proxy backend
- Bitácora vacía → import incorrecto de Vencimiento + serialización enums
- `honorarios-tab.tsx` con `onCreated` no commiteado → TypeScript build error en Vercel
- "0 días" en tiempo de vida → cálculo por fechas calendario ART

## Estado del sprint
- **EXP-BIT-001:** ✅ done
- **DOC-002:** ✅ done
- **DOC-003:** ✅ done
- **TRIAL-001:** ✅ done (pendiente verificar seteo en auth.py al crear estudio)
- **NOTIF-001:** ✅ done (pendiente verificar en Railway — requiere `--build` para instalar apscheduler)

## Pendiente / deuda técnica
- Tests para endpoints de documentos (proxy, merged-pdf, PATCH)
- Verificar APScheduler corriendo en Railway
- Verificar `trial_ends_at` se setea al registrar estudio nuevo
