# Daily Sprint 06 — 2026-04-15

**Sprint:** 06
**Estado:** ✅ Sprint completado en una sesión

---

## Qué se hizo hoy

### Día 1 — UX polish (XS/S)
- ✅ **UX-006** Pre-selección expediente en formularios: `?expediente_id=xxx` → banner informativo + campo bloqueado + cancel vuelve al expediente
- ✅ **UX-003** Centrado forms ABM: `max-w-2xl mx-auto` en clientes, expedientes y vencimientos
- ✅ **UX-004** Search bars expand: `w-52 sm:focus:w-80 transition-all` en expedientes, clientes y vencimientos
- ✅ **UX-005** Shortcut agnóstico: detecta Mac/otros con `navigator.platform`, muestra `⌘K` o `Ctrl+K`

### Día 2 — Notificaciones
- ✅ **NOT-002** Panel desplegable de notificaciones urgentes:
  - Click en pill urgentes abre dropdown con lista
  - Cada ítem navega al expediente (`/expedientes/{id}`)
  - "Marcar vistas" limpia el badge
  - Cierra con click fuera

### Día 3 — CONT-001 backend
- ✅ Modelo `Gasto` con enum `GastoCategoria` + reutiliza `Moneda` de honorarios
- ✅ Migración `743b87a56c88` — `create_type=False` para enum `moneda` ya existente
- ✅ Router `/gastos` con CRUD completo + `/gastos/resumen` para widget
- ✅ Schemas `GastoCreate`, `GastoUpdate`, `GastoOut`, `GastoResumen`
- ✅ Tests TDD: 10/10 — CRUD + resumen + aislamiento tenant

### Día 4 — CONT-001 frontend
- ✅ Tipos `Gasto`, `GastoCategoria`, `GastoResumen` en `api.ts`
- ✅ Página `/gastos` con: filtro por mes, totales ARS/USD, formulario inline, CRUD completo, colores por categoría
- ✅ "Gastos" agregado al sidebar nav

### Día 5 — CONT-002 widget financiero
- ✅ Widget "Este mes" en sidebar del dashboard:
  - Ingresos ARS (honorarios cobrados del mes)
  - Gastos ARS
  - Resultado neto ARS (con color verde/rojo)

### Día 6 — VCT-004 backend
- ✅ `google_calendar_id` columna en `User` — migración `255ad9d19349`
- ✅ Dependencias: `google-auth`, `google-auth-oauthlib`, `google-api-python-client`
- ✅ Router `/auth/google-calendar/*`: connect, callback, calendars, select-calendar, disconnect
- ✅ Router `/vencimientos/sync-calendar` — pushea vencimientos pendientes al calendar elegido
- ✅ `GET /users/me` — perfil con estado de Calendar
- ✅ `GOOGLE_CALENDAR_CLIENT_ID/SECRET/REDIRECT_URI` en config

### Día 7 — VCT-004 frontend
- ✅ Página `/perfil`: datos personales + sección Google Calendar (conectar/desconectar/selector)
- ✅ "Mi perfil" en sidebar nav
- ✅ Vencimientos: botón Sync Calendar activado si `calendarConnected`, muestra resultado
- ✅ Si no conectado: link a `/perfil` con tooltip

---

## Métricas del sprint
- Tests: 91/91 ✅ (10 nuevos de gastos)
- tsc: sin errores ✅
- Migraciones aplicadas: 2 ✅
- Historias completadas: 8/8 (UX-006, UX-003, UX-004, UX-005, NOT-002, CONT-001, CONT-002, VCT-004)

---

## Decisiones técnicas

- `postgresql.ENUM(create_type=False)` para reutilizar el enum `moneda` ya existente en migraciones
- Google Calendar OAuth2 separado del login: cualquier usuario (email o Google) puede conectar su Calendar
- Widget financiero dashboard: calcula ingresos/egresos en el cliente (no nuevo endpoint), para simplificar
- Perfil: `/users/me` devuelve `"***"` en lugar del refresh_token real (seguridad)
- `google_calendar_id` se guarda con `POST /auth/google-calendar/select-calendar?calendar_id=...` (query param) para simplicidad

---

## Blockers
Ninguno.

---

## Para el próximo sprint
- CONT-003: Costos por expediente (rentabilidad del caso)
- CONT-004: Facturación con PDF
- Configurar Google Cloud credentials para activar VCT-004 en real
- Restaurar splash screen a modo producción (un comentario marcado con `// DEV:`)
