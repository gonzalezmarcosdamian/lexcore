# Daily — Sprint 04 — 2026-04-15

## Completado hoy

### HON-001 · Honorarios por expediente ✓
- **Backend:** modelo `Honorario` + `PagoHonorario`, migración Alembic `0916d762e7cd`
- **Backend:** router `/honorarios` con CRUD completo + endpoint `/resumen` para dashboard
- **Tests TDD:** 18 tests (CRUD + pagos + saldo calculado + aislamiento tenant) — todos verdes
- **Frontend:** pestaña "Honorarios" en detalle de expediente con barra de progreso, saldo en tiempo real, form de pago con comprobante

### NOT-001 · Badge urgentes en sidebar ✓
- Badge rojo en ítem "Vencimientos" del sidebar con count de urgentes (< 48hs)
- Pill de urgentes en el header desktop con link directo a `/vencimientos`
- Badge en mobile bottom tab bar

### BUS-001 · Búsqueda global ✓
- **Backend:** endpoint `GET /search?q=` con búsqueda en expedientes (número, carátula) + clientes (nombre, CUIT)
- **Tests TDD:** 9 tests (por tipo, sin resultados, query corta, aislamiento tenant)
- **Frontend:** componente `SearchPanel` en sidebar con debounce 300ms, dropdown de resultados agrupados, navegación con Escape

### VCT-003 · Exportación de calendario ✓
- **Backend:** router `/ical` con 3 endpoints: vencimiento individual, por expediente, próximos N días
- Formato iCal estándar con 3 recordatorios (7 días, 48hs, 2hs)
- **Frontend:** botón "Exportar" en `/vencimientos` con dropdown: "Descargar iCal" (Apple/Outlook) + "Google Calendar" (suscribir URL)

### USR-002 · Email de invitación ✓
- `app/services/email.py` con HTML premium estilo LexCore
- Integrado en `POST /invitaciones` — falla silenciosa si `RESEND_API_KEY` no está configurada
- Variables agregadas al config: `RESEND_API_KEY`, `BASE_URL`

## Estado de tests
- Backend: **67/67 pasando** ✓
- Frontend: **tsc --noEmit sin errores** ✓

### Completado (continuación)

#### Seed de honorarios ✓
- 7 honorarios con datos reales (ARS + USD, estados mixtos de pago)
- Reset actualizado para borrar `pagos_honorarios` + `honorarios` antes de re-seedear

#### Widget de honorarios en dashboard ✓
- Card "Honorarios pendientes" en columna derecha del dashboard
- Muestra saldo ARS, saldo USD y cantidad de expedientes con deuda
- Consume `GET /honorarios/resumen` con loading skeleton

#### Página `/aceptar-invitacion/[token]` ✓
- Valida token contra `POST /invitaciones/aceptar/{token}` al cargar
- Muestra datos de la invitación (nombre, email, rol) antes de confirmar
- Form de creación de contraseña con validación
- Llama a `POST /auth/register-invited` (nuevo endpoint) que crea el user y marca la invitación como usada
- Ruta pública (no en middleware matcher) — no requiere login

#### Backend `POST /auth/register-invited` ✓
- Valida token de invitación (no usado, no expirado)
- Crea usuario con el rol de la invitación
- Marca invitación como usada
- Devuelve JWT igual que `/auth/login`

#### Documentación y deps ✓
- `resend==2.3.0` agregado a `requirements.txt`
- `RESEND_API_KEY` + `BASE_URL` documentados en `.env.example`

## Estado final Sprint 04
- Backend: todos los tests pasando ✓
- Frontend: tsc --noEmit sin errores ✓
- Flujo de invitación completo: email → link → `/aceptar-invitacion/[token]` → registro → login
